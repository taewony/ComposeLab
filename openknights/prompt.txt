# OpenKnights Project - Gemini CLI Prompt

## Done by today (2025년 7월 14일 월요일):
- OpenKnights의 핵심 데이터 모델 (Contest, Project, User, Participant, Contributor, Tag, ContestSchedule, ContestStatus, ProjectPhase, ProjectRole)을 `openknights/core/model`에 정의했습니다.
- 새로운 데이터 모델을 기반으로 한 가짜 데이터 (`FakeOpenKnightsData.kt`)를 `openknights/core/testing`에 생성했습니다.
- `openknights:feature:contest` 모듈을 새로 생성하고, `ContestListScreen`, `ContestListViewModel`, `ContestListNavigation`의 기본 구조를 구현했습니다.
- 모든 `openknights` 서브 모듈의 `build.gradle.kts` 파일을 `com.openknights.app` 네임스페이스, 올바른 `compileSdk`/`minSdk`/`jvmTarget` 설정, Hilt 의존성을 포함하도록 업데이트했습니다. (build-logic 사용하지 않음)
- `ComposeLab/settings.gradle.kts`에 `openknights:feature:contest` 모듈을 포함시켰습니다.
- `ComposeLab/gradle/libs.versions.toml`에 Hilt, `kotlinx.immutable`, `compose.shimmer` 관련 버전 및 라이브러리 정의를 추가했습니다.
- `openknights/app/MainActivity.kt`를 수정하여 `ContestListScreen`을 시작 화면으로 설정하고, 기존 화면 참조를 제거했습니다.
- `openknights/app/src/main/res/values/strings.xml` 파일을 생성했습니다.
- `openknights/app/build.gradle.kts`를 업데이트하여 올바른 `applicationId`, Hilt 플러그인, `feature:contest` 의존성을 포함하도록 했습니다.
- `MainActivity.kt`에 `@AndroidEntryPoint` 어노테이션을 추가하고, `AndroidManifest.xml`에 `android:name=".OpenKnightsApplication"` 속성을 추가했습니다.
- `strings.xml` 파일의 XML 선언 오류를 수정했습니다.
- 모든 `openknights` 서브 모듈의 소스 파일들이 `com.openknights.app` 패키지 구조 아래에 위치하도록 디렉토리 이동 및 패키지 선언을 일관되게 변경했습니다.

## Next Steps:
- `SessionListScreen`을 `ProjectListScreen`으로 리팩토링합니다.
- `SessionDetailScreen`을 `ProjectDetailScreen`으로 리팩토링합니다.
- `ContestListScreen`에서 `ProjectListScreen`으로 이동하는 내비게이션 로직을 구현합니다.

Now, I will refactor SessionListScreen to ProjectListScreen. This involves
  several steps:

   1. Update `settings.gradle.kts`: Include the new
      openknights:feature:project module.
   2. Update `openknights/feature/project/build.gradle.kts`:
       * Change namespace to com.openknights.app.feature.project.
       * Update dependencies to refer to openknights modules.
       * Ensure jvmTarget = "17" is set.
   3. Rename files and classes:
       * Rename SessionScreen.kt to ProjectListScreen.kt.
       * Rename SessionListViewModel.kt to ProjectListViewModel.kt.
       * Rename navigation/SessionNavigation.kt (or similar) to
         navigation/ProjectListNavigation.kt.
       * Rename classes inside these files accordingly.
   4. Update package declarations: Change package
      com.droidknights.app.feature.session to package
      com.openknights.app.feature.project in all relevant Kotlin files within
      the project module.
   5. Update imports: Change import com.droidknights.app.feature.session.* to
      import com.openknights.app.feature.project.* and update imports for
      core:model classes.
   6. Refactor code logic:
       * Change Session references to Project.
       * Change SessionListViewModel logic to fetch Project data.
       * Adapt UI components to display Project information.

Now that all the files in openknights/feature/project have been refactored,